model : 'ode' # options: absolute, mpoly_2, poly_2, residual, RKNN, way_2, ode
ODE_solver: False  # Can only be set when model = 'ode'; False if default (dopri5); options: False or 'rk4'
obstacle : False # True if pushing with obstacle, False if free pushing
base_dir : '/mnt/NDE-based-Robot-Learning-Dynamics' #*NEEDS TO BE CHANGED BEFORE RUNNING*
step : 'single'      # Single step or Multi step; options: single, multi
dataset: 'panda'    # options: panda, baxterfk, baxterik